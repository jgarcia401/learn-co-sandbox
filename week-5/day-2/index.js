
function fiftydiscountPrice(nonDiscountPrice) {
  
  return nonDiscountPrice / 2
}

// returns are used to save results outside of functions

function makeANumberAPrice(someNumber) {

  console.log('$' + someNumber + '.00')
  
}

const price = 20

const discountPrice = fiftydiscountPrice(price)

// 20 => 10
fiftydiscountPrice(price)

//
makeANumberAPrice()








//Variables

  // let tacosAvailble = 3 

  // let weather = 'sunny'

  // // if the waeather is good, and there are more than 0 tacos available, we want to do something

  // if (weather === 'sunny' && tacosAvailble> 0) {
  
  //   // if there are 2 tacos availble, aler with a message saying there's 2 left
  //   // otherwise say, only 1 left
  //   // nesting if statement - put if statements inside other if statements
  //   // chaining combining multiple if statements before the else
  
  //   if (tacosAvailble === 2) {
  //     alert('there are only 2 left')
  //   } else {
  //   alert('1 left')
  // }
  // }
